# Vendored plugin deps are bootstrap-installed by the desktop boot
# (ensureVendorDeps). Peers come from the profile's loader, and node-pty is
# rebuilt for Electron by pty-rebuild.mjs — its own npm build script is not
# needed (and would fetch a Node-ABI binary we never load).
auto-install-peers=false
